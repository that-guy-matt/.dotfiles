# local config goes in include
[include]
    path = ~/.gitconfig.local

[core]
    editor = vim
    excludesfile = ~/.gitignore

[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
