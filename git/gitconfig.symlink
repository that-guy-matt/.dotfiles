# local config goes in include
[include]
    path = ~/.gitconfig.local

[core]
    editor = vim
	excludesfile = /home/matthew/.gitignore_global

[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
